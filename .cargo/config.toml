# Cargo 配置文件
# 用于优化编译体积和性能
#
# 注意：编译优化配置已移至 Cargo.toml 的 [profile.release] 中
# 这里不再设置全局 rustflags，避免影响 dev/test/clippy 等构建模式
# 
# 如果需要全局配置，可以使用以下方式（但不推荐，因为会影响所有构建）：
# [build]
# rustflags = ["-C", "link-arg=-fuse-ld=lld"]  # 使用更快的链接器（如果可用）


